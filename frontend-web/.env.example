VITE_API_BASE_URL=http://localhost:8080/api

# Optional: direct streaming base URL. If not set, the UI will derive it from
# VITE_API_BASE_URL by removing the trailing /api and appending /stream
# Example: http://localhost:8086/stream
VITE_STREAM_BASE_URL=http://localhost:8080/api/stream
