spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/video_db

#  elasticsearch:
#    uris: http://elasticsearch:9200

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-discovery:8761/eureka/

  instance:
    prefer-ip-address: true